import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import Layout from '.'

<Meta title="Layout" component={Layout} />

# Layout

<Canvas>
  <Story name="Basic" args={{ children: 'Съешь ещё этих мягких французских булок, да выпей же чаю', isHome: false }}>
    {args => <Layout {...args} />}
  </Story>
</Canvas>

Обёртка для компонентов страниц. Включает в себя хэдер и футер, оборачивает `children` в **main**. Флаг `isHome` пробрасывается до хэдера.

<ArgsTable story="." />

## Home page

<Canvas>
  <Story name="Home page" args={{ children: 'Съешь ещё этих мягких французских булок, да выпей же чаю', isHome: true }}>
    {args => <Layout {...args} />}
  </Story>
</Canvas>
